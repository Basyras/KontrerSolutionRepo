@{
    var duration = EndTime - StartTime;
    string unknownStyle = $"width: {DurationViewHelper.GetDurationAsRem(duration, Scale)}rem;";
}
<MudTooltip Arrow="true">
    <ChildContent>
        <horizontalUnknownDurationBoxView style="@unknownStyle">
            <unknownDurationLine/>
        </horizontalUnknownDurationBoxView>
    </ChildContent>
    <TooltipContent>
        <toolTipContent>
            name: unknown duration
            <br />
            start: @StartTime.Millisecond
            <br />
            end: @EndTime.Millisecond
            <br />
            dur: @duration.TotalMilliseconds ms
        </toolTipContent>
    </TooltipContent>
</MudTooltip>

@code {
    [Parameter] public DateTimeOffset StartTime { get; init; }
    [Parameter] public DateTimeOffset EndTime { get; init; }
    [Parameter] public double Scale { get; set; }
}
