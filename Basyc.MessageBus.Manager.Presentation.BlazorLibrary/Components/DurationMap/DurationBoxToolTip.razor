@using Basyc.Diagnostics.Shared.Durations
@using Basyc.MessageBus.Manager.Presentation.BlazorLibrary.Shared.Helpers

<toolTipDurationName>
    @Service.ServiceName: @Name
</toolTipDurationName>
<toolTipDurationDuration>
    started: @TimeConverter.TimeToText(StartTime);
    <br />
    lasted: @TimeConverter.DurationToText(Duration);
</toolTipDurationDuration>

@code {
    [Parameter] public string Name { get; set; }
    [Parameter] public DateTimeOffset StartTime { get; set; }
    [Parameter] public TimeSpan Duration { get; set; }
    [Parameter] public ServiceIdentity Service { get; set; }
}
