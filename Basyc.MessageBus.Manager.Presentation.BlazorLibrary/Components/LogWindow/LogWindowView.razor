@using Basyc.MessageBus.Manager.Application.ResultDiagnostics

<logWindowView>
    @for (int logEntryIndex = 0; logEntryIndex < DiagnosticsContext.LogEntries.Count; logEntryIndex++)
    {
        var logEntry = DiagnosticsContext.LogEntries[logEntryIndex];
        <LogEntryView LogEntry="logEntry" AlternateColor="logEntryIndex % 2 != 0" />
    }
</logWindowView>


@code {
    [Parameter] public RequestDiagnosticContext DiagnosticsContext { get; set; }
}
