<collectionCountContainer>
    @if (IsLoading)
    {
        @:-
    }
    else
    {
        @Collection?.Count();
    }
</collectionCountContainer>

@code {
    [Parameter] public IEnumerable<object> Collection { get; set; }
    [Parameter] public bool IsLoading { get; set; } = false;
}